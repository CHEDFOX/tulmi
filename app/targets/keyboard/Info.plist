<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CFBundleDisplayName</key>
  <string>Tailzu</string>
  <!-- Required so the keyboard can record voice for dictation (Full Access). -->
  <key>NSMicrophoneUsageDescription</key>
  <string>Tulmi uses the microphone to turn your speech into clean text.</string>
  <key>NSExtension</key>
  <dict>
    <key>NSExtensionAttributes</key>
    <dict>
      <key>IsASCIICapable</key>
      <false/>
      <key>PrefersRightToLeft</key>
      <false/>
      <key>PrimaryLanguage</key>
      <string>en-US</string>
      <!-- Required so the keyboard can reach the Tulmi backend for Refine. -->
      <key>RequestsOpenAccess</key>
      <true/>
    </dict>
    <key>NSExtensionPointIdentifier</key>
    <string>com.apple.keyboard-service</string>
    <key>NSExtensionPrincipalClass</key>
    <string>$(PRODUCT_MODULE_NAME).KeyboardViewController</string>
  </dict>
  <!--
    App Transport Security: defaults are strict (HTTPS only with TLS ≥ 1.2),
    which matches the production backend at https://api.tailzu.space. We do
    NOT set NSAllowsArbitraryLoads here — that would ship cleartext-everywhere
    permission to App Store users and is a review red flag.

    For local development against a plain-HTTP PC backend, add an exception
    domain (e.g. "localhost") to YOUR working copy and do not commit it.
  -->
</dict>
</plist>
